CREATE TABLE items (
    item_id int,
    iname varchar(255),
    idesc varchar(255),
    PRIMARY KEY (iid)
);

insert into items (item_id, iname, idesc) VALUES ('0', 'testing', 'testing 123');